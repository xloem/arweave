dist: bionic
language: erlang
addons:
    apt:
      packages:
      - sqlite3
      - libgmp-dev
install:
    - epmd -daemon
otp_release:
    - 24.1
branches:
    except:
    - master

before_script: ./rebar3 ct --cover --sname=master@localhost

script:
    - ./bin/test
